# 英雄战歌2 开发执行文档（同步版）

## 0. 当前目标
- 当前主线：核心玩法调整为“站台守护者”：原英雄运行时定位调整为中心塔，新增可移动守护者负责拾取/跑图交互
- 当前原则：第一阶段先不改系统显示名称与表字段命名，避免多语言前重复劳动；内部规则先按“塔 + 守护者”落地
- 当前实现顺序：先把旧英雄固定为塔并接管攻击/属性/神权/剑盾成长，再补守护者移动拾取；`MD-03` 剑&盾成长继续保留为后续祭坛升级基础

## 1. 当前数据流（已稳定）
1. 策划维护：`res://英雄战歌2.xlsx`
2. 项目维护：`res://Excel/*.xlsx`
3. 一键导出：`res://Excel/一键导出JSON.bat`
4. 运行时：`res://data/tables/*.json`
5. 读取入口：`DataTable` 单例

## 2. 表命名规则（已执行）
- 正式维护表：`英文结构名#中文名.xlsx`
- 例如：
- `heroes#英雄.xlsx`
- `attributes#属性.xlsx`
- `attribute_formulas#属性公式.xlsx`
- `actions#动作.xlsx`
- `runtime_constants#运行时常量.xlsx`

## 3. 进度看板

### 3.1 已验收
- `xlsx -> json` 导出链路
- `DataTable` 单例读取 `json`
- `F6` 热重载并重开场景
- 英雄表基础读取（`heroes.json`）
- 英雄模型按 `model_id` 加载

### 3.2 已完成（待验收）
- `MD-11` 第一版属性系统
- 新增属性快照容器：`combat_stats.gd`
- 新增公式结算层：`attribute_system.gd`
- 英雄三维与主属性接入：`primary_attr/base_str/base_agi/base_int`
- 玩家入场改为读取“最终属性”而非直接读原始字段
- 普攻伤害走属性公式（护甲/穿透/增伤/最终伤害/暴击）
- 敌人受击接入 `take_projectile_hit`，按公式结算伤害
- 玩家受伤接入护甲/魔抗减伤换算
- 敌人改为 `enemies.json` 表驱动生成（`id` 数字序列、`enemy_type` 枚举、`model_id`、`armor`、`magic_resist`、`attack_interval`）
- 怪物/英雄模型改为 `model_id -> assets/heroes/models_3d/index.json` 配置驱动
- 战斗表现主链路切换为纯 `3渲2`（`SubViewport` 渲染 3D，再输出到 2D 战斗层）
- 英雄基线冻结为 3 名（`heroes#英雄.xlsx`），技能列改为占位字段（不参与战斗）
- `MD-04` 第一版波次系统接入：
- 按 `waves.json` 顺序刷怪
- 支持 `enemy_id` / `boss_id` 按 `enemies.id` 驱动
- 波次按表字段推进（`total/spawn_rate/duration`）
- `duration=80` 时即 80 秒后进入下一波
- 增加场上怪物上限阈值（默认 50）
- 顶部中间新增波次 HUD（第X波 / 当前怪物数 / 波次倒计时）
- `MD-04` 第二版波次节奏接入：
- 开局加入准备期，波次切换加入波间过渡
- 顶部 HUD 区分“准备期 / 战斗中 / 结算过渡 / 全部完成”
- 中央新增波次提示条，显示“即将开始 / 开始 / 完成”
- 波次支持“刷完且清场”提前结束，否则按 `duration` 自动结算推进
- 修正 `boss_id = null` 的兼容判定，避免普通波误判 Boss
- 波次表增强规则接入：
- `wave_type` 枚举定义波次类型：`1=基础波次`，`2=Boss波次`
- `spawn_interval` 可直接定义“每隔 X 秒刷 1 只主怪”
- `special_enemy_id / special_spawn_time / special_spawn_count` 可定义“在第 N 秒插刷特殊怪”
- `boss_id` 仅在 `wave_type=2` 时生效；若 Boss 波未填写 `boss_id`，则回退到默认 Boss 池
- 正上方波次信息下方新增大型 Boss 血条，Boss 存活时实时显示生命
- `MD-05.5` 第一版杀敌奖励接入：
- 怪物死亡按 `enemies.json` 的 `gold_reward / exp_reward` 发放金币与经验
- 奖励兼容属性表中的 `gold_gain_percent / exp_gain_percent / bonus_gold_per_kill / bonus_exp_per_kill`
- 底部战斗 HUD 显示当前金币 / 经验
- `TAB` 全属性面板同步显示金币 / 杀敌数 / 等级
- 当前经验先做累计展示，等级暂固定 1，后续与成长/技能系统统一接入
- `MD-05` / `MD-03` 前置接口已接入：
- `player.gd` 新增运行时 `runtime_bonus_values`
- 支持 `set/merge/add/clear bonus_values` 动态刷新最终属性
- 刷新时保留当前生命/法力，不重置整场战斗进度
- `game.gd` 提供统一入口，后续卡牌/武器/奖励系统直接写入即可
- `MD-05.5` 第二版阈值奖励接入：
- 读取 `kill_rewards.json`，按 `threshold` 自动发奖
- 已支持 `gold / card / attribute / weapon` 四种奖励类型
- 卡牌奖励会从 `cards.json` 随机抽取并转成 `bonus_values`
- 属性奖励支持解析如 `attack_damage:+10%`
- 武器奖励会先转成被动属性加成接入 `bonus_values`
- 主动武器的独立攻击行为仍放在后续武器系统正式接入
- 中央提示会显示阈值奖励结果（金币 / 卡牌 / 属性 / 武器）
- 金币 / 经验怪物掉落改为拾取物表现：
- 怪物死亡后会掉落金币球 / 经验球
- 拾取物会短暂弹开，再自动吸附到玩家
- 真正到账时机改为“拾取时”，HUD 与属性面板同步刷新
- 击杀敌人时新增爆金币特效
- 金币 / 经验到达玩家时新增到账飘字
- 玩家对敌人造成伤害时新增红色伤害飘字
- 奖励映射与掉落手感已细化一轮：
- 卡牌/武器奖励优先映射到属性表已有语义 ID（攻速/暴击/武器伤害系数等）
- 百分比属性兼容 `12` / `12%` 两种填法，减少策划表调整时的对接成本
- 掉落改为按数量拆分为多颗拾取物，吸附速度随距离递进
- 拾取物吸附逻辑修正为“短暂弹开后全图吸向玩家”，避免远距离击杀后不追人的问题
- 中央奖励提示改为排队播放，减少连续奖励时互相覆盖
- 等级 / 经验进度接入：
- 新增正式表 `levelup#等级.xlsx` -> `data/tables/levelup.json`
- 英雄等级范围改为 `1~100`，初始 `1` 级
- 当前经验改为累计经验，按 `levelup` 表自动推导等级
- 英雄表新增 `str_growth / agi_growth / int_growth`，升级时同步提升基础三维
- 肖像面板在蓝条下方新增经验条，显示当前级经验进度
- 升级时会刷新玩家最终属性，等级不再只是展示值
- 等级展示已收口，只保留“左上英雄名旁等级”，去掉经验条 / 底部 HUD / TAB 面板中的重复等级显示
- 修复 `HeroCatalog` 解析异常（历史脏字符导致类加载失败）
- 死亡复活逻辑接入：
- 玩家死亡后不再暂停整局
- 屏幕出现 90% 透明度的淡灰色蒙版作为遮挡
- 默认 5 秒后复活；等级超过 50 级后改为 10 秒
- 复活后自动隐藏蒙版，并恢复角色行动
- 窗口 / 视野自适应接入：
- 项目窗口改为可调整大小，最大化后不再锁死在 `1440x810`
- 玩家相机在窗口尺寸变化时会自动重算缩放，战斗主体随窗口放大
- 卡牌解析升级为“描述驱动”：
- 不再只依赖旧的 `stat / value` 单字段，优先解析 `description`
- 一张卡可同时提供 2~3 条直接属性，描述中的多条数值会同时转成 `bonus_values`
- 支持 `主属性 / 副属性 / 基础主属性 / 基础副属性` 按当前英雄主属性自动映射
- 支持拆出多个 `唯一被动 / 被动` 文本并缓存，后续正式被动系统可直接复用
- 英雄头像字段兼容“资源ID写法”：
- `heroes#英雄.xlsx` 的 `portrait_icon` 支持填写 `1001` 这类短 ID
- 运行时会自动映射到 `res://assets/heroes/portraits/{id}.png`（也兼容完整 `res://` 路径）
- 已验证 `portrait_icon=1001` 可正常加载，不再出现 `res://1001` 找不到资源
- 新增 `TAB` 全属性面板：
- 按 `TAB` 打开/关闭
- 打开时暂停游戏，再按一次恢复
- 属性名称来自 `attributes` 表（当前为全属性，不再只限外显）
- 面板布局为 3 列多行
- 排序支持 `sort_weight`，无该字段时按表内顺序
- 3D 战斗主链路与动作表已接入（纯3D，无2D回退）：
- `enemy_sprite_animator.gd` 改为纯 3D 转发器，移除序列帧分支
- `hero_model_catalog.gd` / `enemy.gd` 仅走 `configure_model_id`，缺配置即告警
- 新增 `Excel/actions#动作.xlsx` -> `data/tables/actions.json`
- 新增 `model3d_action_catalog.gd`，支持按 `model_id` 读取 `idle/move/hit/stun/death/spawn/victory`
- 攻击仍使用 `assets/heroes/models_3d/index.json` 里的 `attack_plan/hit_frames`
- 攻击编排字段升级为“可选三段”：`windup_animation_name / release_animation_name / recover_animation_name`
- 兼容旧配置：仅有 `animation_name` 的历史数据会自动映射到 `release` 段（无需返工重配）
- 三渲二后处理已接入：Toon / 描边 / 可选像素化；并统一主光/补光方向与阴影强度
- 跳跃系统已恢复并升级为“位移跳跃”：
- `Space` 触发，动作表新增 `jump` 状态，支持 `jump_1 -> jump_2 -> jump_3`
- 跳跃总时长限制 `<=0.3s`，未落地前禁止移动与攻击
- 跳跃期间按朝向前冲（`jump_forward_distance` 可配置），可越过围住玩家的敌人
- 已修复跳跃相关脚本告警（变量作用域与命名冲突）
- 选择界面渲染与输入链路已做结构化优化（不改玩法规则/表规则/JSON链路）：
- 新增 `SelectionState`，统一管理模板索引、血脉索引、UI 脏标记与重建判定
- 选择界面刷新改为“按状态重建”：仅在模板变更或选项集变化时重建血脉按钮，其余仅更新选中态与摘要
- 预览模型改为“签名复用”：模型签名不变时不重复销毁/实例化，减少切换时的无效构建开销
- 预览镜头构图改为“角色本体优先”：自动取景时不再把血脉武器网格计入包围盒，切换血脉时人物画面尺寸更稳定
- 预览镜头进一步改为“固定参数框架”：选人界面使用固定相机参数与居中逻辑，不再改写模型原始比例，保证与 F6 配置一致
- 血脉模型覆盖增强：除武器挂点外，额外支持 `hair/skin/cloth/hat` 的 `preset + tint` 着色覆盖，保证不同血脉外观差异可见
- 选人预览切换体验增强：每次切换模板/血脉后先播放一次 Victory 动作，再自动回到 Idle 循环
- 选人预览镜头继续微调：镜头距离上调约 10%，并抬高取景目标，减少头饰被裁剪
- 选人信息文案收口：去除“技能组”残留显示；“人物模板”改为“人物”；“额外技能”改为“血脉效果”
- 血脉卡 Tooltip 收口：移除重复血脉名与技能组文案，血脉图标放大，Tooltip 延迟下调至 `0.08s`
- 血脉 Tooltip 继续收口：移除“外观：武器方案 xxxx”技术描述，避免对玩法无关信息干扰
- 主界面神权背包收口：左侧卡牌取消 Tooltip；右侧标题改为当前卡牌名；当前详情对应卡牌在左侧以金色边框高亮
- 技能按钮渲染新增签名缓存，避免同内容重复重绘
- Label 文本更新改为“值变更才写入”，降低选择界面高频刷新时的 UI 开销
- Excel 角色选择三表已统一重命名（英文键不变，仅中文名增强可读性）：
- `heroes#英雄.xlsx`
- `bloodlines#血脉.xlsx`
- 已移除旧方案表：`weapon_loadouts#武器组合.xlsx`、`template_weapon_options#模板武器选项.xlsx`
- 角色选择主属性规则已调整：
- `heroes#英雄.xlsx` 移除 `primary_attr`
- `bloodlines#血脉.xlsx` 新增 `primary_attr(str/agi/int)`，运行时按所选血脉决定主属性
- 角色选择基础属性与模型规则已调整：
- `heroes#英雄.xlsx` 移除基础战斗属性列（`base_str/base_agi/base_int/base_hp/base_mana/move_speed/attack_damage/attack_interval/attack_range`）
- `bloodlines#血脉.xlsx` 新增并维护上述基础战斗属性，运行时按血脉覆盖模板默认值
- 角色选择关系改为模板直连血脉：
- `heroes` 新增 `bloodline_ids`（逗号分隔血脉ID，例如 `1,2,5`）
- 运行时按 `heroes.bloodline_ids -> bloodlines.id` 构建可选血脉
- 模型改为由血脉统一提供：
- `heroes` 不再维护 `model_id`
- `bloodlines.model_id` 同时用于预览与战斗模型来源
- `bloodlines.preview_model_id` 已移除
- 角色选择字段继续收口（2026-04-16）：
- `heroes#英雄.xlsx` 移除 `preview_model_id`、`body_color`、Q/W/R 技能列
- `heroes` 保留 `portrait_icon`、`accent_color` 与 `bloodline_ids`
- `bloodlines#血脉.xlsx` 新增 Q/W/R 技能名称/描述/icon 与 `skill_group_id/skill_group_name`
- `heroes` 与 `bloodlines` 均新增 `ban`（或 `Ban`）字段，运行时会过滤禁用项

### 3.3 当前遗留（下一步处理）
- 继续细化卡牌/武器具体数值映射，和策划表做一轮最终对齐
- 继续打磨掉落物手感与表现节奏（金币爆开数量 / 吸附速度 / 飘字密度）
- 继续补 UI 收口与信息层级，避免战斗 HUD 冗余
- 继续补完整波次表，并把 `wave_type / boss_id / 插刷规则` 铺满
- 继续补卡牌中“事件触发 / 条件触发 / 累计收益型”被动的正式执行逻辑
- 继续推进结算主流程：最终波清场胜利 / Boss波超时失败 / 结算发放局外货币 / 返回主界面
- 技能系统（英雄/武器/卡牌）最后统一接入

## 4. MD 分阶段状态

### MD-01 数据系统
- 状态：已完成并验收

### MD-02 玩家系统
- 状态：原型完成，正在向属性系统重构

### MD-03 武器系统
- 状态：下一阶段正式开始；前置金币、属性刷新、武器表读取、被动属性加成入口已存在，优先实现“剑&盾”局内金币成长武器闭环

### MD-04 波次系统
- 状态：已完成第二版，并开始接入“表驱动 Boss 波次 / 定时插刷 / Boss血条”增强规则

### MD-05 卡牌系统
- 状态：原型接入（奖励获得后可转为属性加成，正式选牌 UI 未开始）

### MD-05.5 杀敌奖励
- 状态：已完成金币/经验 + 阈值奖励第二版（已补全图吸附 / 爆金币 / 飘字表现，待继续打磨）

### MD-06 UI 系统
- 状态：进行中（已加入全属性面板原型 + 经验条，并完成一轮等级信息去重 / 死亡遮罩 / Boss血条）

### MD-07 挑战系统
- 状态：未开始

### MD-08 英雄系统
- 状态：选人原型完成，3英雄普攻版已定稿，技能正式化未开始（头像字段已支持 `ID/完整路径` 双模式）

### MD-09 奖励系统
- 状态：未开始

### MD-10 局外成长
- 状态：未开始
- 目标定义补充：
- 这里的“存档”不是主界面展示战绩
- 而是结算后发放并写入局外资源（例如货币A），玩家可在局外抽奖/成长，再把获得的属性加成带入下一局

### MD-11 属性计算
- 状态：进行中（第一版已完成，等级已接入属性重算）

## 5. 本轮改动涉及文件
- `res://attribute_system.gd`
- `res://combat_stats.gd`
- `res://hero_data.gd`
- `res://hero_catalog.gd`
- `res://player.gd`
- `res://projectile.gd`
- `res://enemy.gd`
- `res://enemy_data.gd`
- `res://enemy_catalog.gd`
- `res://enemy_sprite_animator.gd`
- `res://model3d_actor_animator.gd`
- `res://model3d_action_catalog.gd`
- `res://game.gd`
- `res://game.tscn`
- `res://systems/selection_state.gd`
- `res://systems/wave_runtime.gd`
- `res://systems/runtime_enemy_snapshot.gd`
- `res://systems/data_table_provider.gd`
- `res://systems/template_bloodline_option_index.gd`
- `res://systems/weapon_growth_runtime.gd`
- `res://systems/card_choice_drop_runtime.gd`
- `res://systems/level_runtime.gd`
- `res://systems/combat_info_formatter.gd`
- `res://systems/kill_reward_amount_calculator.gd`
- `res://systems/reward_pickup_splitter.gd`
- `res://systems/threshold_reward_picker.gd`
- `res://systems/card_icon_resolver.gd`
- `res://systems/table_value_utils.gd`
- `res://systems/card_display_text.gd`
- `res://systems/card_description_text.gd`
- `res://systems/regex_cache.gd`
- `res://systems/transient_message_runtime.gd`
- `res://pickup.gd`
- `res://pickup.tscn`
- `res://data/tables/levelup.json`
- `res://data/tables/actions.json`
- `res://Excel/levelup#等级.xlsx`
- `res://Excel/actions#动作.xlsx`
- `res://data/tables/waves.json`
- `res://Excel/waves#波次.xlsx`
- `res://Excel/heroes#英雄.xlsx`

## 6. 你现在怎么验收
1. 进战斗后按 `TAB`：应显示全属性面板，并暂停游戏
2. 再按 `TAB`：应关闭面板并继续游戏
3. 开局应先进入准备期，再显示“第1波开始”
4. 每波结束后应进入短暂波间过渡，再提示下一波
5. 当一波怪物已刷完且场上清空时，应可提前进入过渡；否则按 `duration` 自动推进
6. `wave_type=2` 的波次应被识别为 Boss 波次，不再依赖波次序号写死规则
7. 若波次表填写 `special_enemy_id / special_spawn_time / special_spawn_count`，应能在指定秒数插刷特殊怪
8. Boss 波时，正上方波次信息下方应出现大型 Boss 血条
9. 玩家死亡后不应暂停整局，而应出现淡灰色遮罩与复活倒计时
10. 50 级及以下复活时间应为 5 秒；超过 50 级后应为 10 秒
11. 复活后遮罩应消失，玩家恢复行动
12. 击杀怪物后，底部 HUD 的金币/经验应增长
13. 击杀数达到 `kill_rewards.json` 阈值后，应自动弹出奖励提示
14. 怪物死亡后应掉落金币球 / 经验球，并自动吸附到玩家
15. 蓝条下方应显示经验条，并随拾取经验球推进
16. 达到升级阈值后，英雄等级应提升，且只在左上肖像名称旁显示等级
17. 击杀带金币敌人时应出现爆金币特效
18. 金币到达玩家后应显示金黄色 `+数值` 飘字
19. 玩家命中敌人时应显示红色伤害飘字
20. 远距离击杀敌人后，金币球 / 经验球也应能稳定追向玩家
21. 获得卡牌 / 属性 / 武器奖励后，`TAB` 面板中的相关属性应发生变化
22. 观察玩家打怪：同样攻击下，不同敌方护甲时伤害应可调整
23. 观察玩家受伤：护甲/魔抗提高后应降低承伤
24. 修改 `Excel/actions#动作.xlsx` 后导出，应生成 `data/tables/actions.json`
25. 为某个 `model_id` 填写 `idle/move` 动作名后，战斗内应能切到对应动作
26. 为某个 `model_id` 填写 `spawn` 动作名后，单位出场时应优先播放该动作
27. 开关 `Model3DActorAnimator` 的 `enable_pixelate_sampling` 后，应可看到像素化采样差异

## 7. 下一步（已排好）
1. `站台守护者` 最小迁移：旧英雄固定为中心塔，新守护者移动拾取，敌人优先攻击塔，玩家普攻迁移为塔自动攻击。
2. `MD-03` 收口：在“塔”语义下继续完成剑&盾局内金币成长闭环（成长表、扣金币、属性刷新、局内 UI）。
3. 性能与资源收口：继续拆 `game.gd` 热点逻辑；当前优先级为卡牌描述解析/被动执行、英雄血脉选择数据装配、奖励落地流程；删除旧图集/旧模型包残留时只保留 F6 与运行时直接引用的资源。
4. `MD-04` 收口：完成前 10 波全量规则验证（普通波固定时长 / Boss波击杀提前结束 / Boss波超时失败）。
5. 结算闭环：完成“胜利/失败面板 -> 发放并写入局外货币/成长存档 -> 返回主界面”全链路。
6. `MD-05` 收口：接入卡牌条件触发与累计型被动执行，并与 `bonus_values` 正式打通。
7. 稳定后再进入技能系统（塔/遗物/战利品统一接入，UI 旧命名可先保留）。

## 8. 新对话恢复口令
按这句话可直接续接：

“按 `英雄战歌2.md` 当前同步版继续：先完成 `MD-04` 前10波验证与结算闭环（胜利/失败/局外货币存档/回主界面），再做 `MD-05` 卡牌条件/累计被动执行接入，最后统一打磨 HUD 与掉落手感，技能系统保持最后接入。”

更新口令：

“按 `英雄战歌2.md` 当前同步版继续：当前主线切到第14节站台守护者最小迁移。先不改系统显示名称和表字段，旧英雄运行时改为中心塔，新守护者无敌、无普攻、负责移动拾取；敌人第一阶段全部优先攻击塔；神权/剑盾/武器槽/波次规则先保持现状，只把属性和收益落到塔。完成最小迁移后，再回到 `MD-03` 剑&盾局内金币成长、`MD-04` 前10波和结算闭环。”

## 9. 代码体检（2026-04-13）
### 9.1 已检查结论
- 启动与脚本解析：通过（`Godot_v4.6.1-stable_win64_console.exe --headless --path E:\\Godot\\vampire-like-demo --quit-after 5`）
- 表驱动头像链路：通过（`portrait_icon=1001` 可自动映射到头像目录）
- 当前主流程（3英雄普攻 / 波次 / 奖励 / 复活 / UI）：可运行

### 9.2 本轮已修复
- 修复窗口可调整大小设置缺失：`project.godot` 已恢复 `window/size/resizable=true`
- 修复卡牌描述解析缓存风险：移除 `_parsed_card_effects` 行级缓存，避免切换英雄主属性后出现“主属性映射陈旧”

### 9.3 仍需后续收口（非阻塞）
- `player.gd` 历史编码脏字符注释已清理（已转为正常 UTF-8 文本）
- `owned_card_passives` 当前仅做缓存与读取接口，尚未进入正式被动执行链
- `game.gd` 体量仍较大（约 200KB），但已开始按纯运行时职责拆分；后续建议继续拆卡牌解析 / 英雄血脉选择 / 奖励落地流程

### 9.4 新对话建议口令（更新）
“按 `英雄战歌2.md` 第9节代码体检结果继续：先完成 `MD-04` 前10波表驱动收口，再做 `MD-05` 卡牌条件/累计被动执行接入，最后进行 HUD 与掉落手感统一打磨，技能系统仍保持最后接入。”

## 10. 3D战斗视觉编辑器（新增）

- 工具根目录：`res://combat_visual_editor/`
- 场景入口：`res://combat_visual_editor/model_viewer/model_viewer.tscn`
- 作用：可视化组合 AI 角色/武器/动作，调姿态，编排攻击触发帧
- 生成目录：`res://assets/heroes/models_3d/`
- 自定义模型导入（2026-04-27 新流程）：
- 人物与动作源目录：`E:\Godot\model_3D\Ai_Model`
- 武器源目录：`E:\Godot\model_3D\Ai_weapons`
- 一键脚本：`E:\Godot\model_3D\一键处理F6模型资源.bat`
- 动作缓存目录：`E:\Godot\model_3D\F6_Cache`
- F6 资源池直接读取 AI 模型目录，按文件夹组织角色；`*_benti.glb/fbx` 为默认本体，其他 GLB/FBX 作为同角色动作合并
- 非本体动作文件会转成保留骨架、剔除网格/材质/贴图的 animation-only GLB 缓存，供 F6 快速预览
- 高低模规则（2026-04-28）：
- `一键处理F6模型资源.bat` 同时完成“原始模型/动作缓存处理 + 战斗低模生成”
- 人物本体低模输出命名：`*_low_benti.glb`
- 武器低模输出命名：`*_low.glb`
- 战斗人物低模预算：最多约 `5000` 三角面
- 战斗武器低模预算：最多约 `1000` 三角面
- F6 与选人界面使用高模，便于检查细节与展示；战斗运行时优先使用低模，降低同屏怪物压力
- F6 下拉列表只显示高模源资源，不显示 `_low` 运行时资源，避免误选低模继续配置
- F6 保存配置时保留高模字段：`character_path / right_weapon_path / left_weapon_path`
- F6 保存配置时自动补运行时字段：`runtime_character_path / runtime_right_weapon_path / runtime_left_weapon_path`
- 若缺少低模文件，运行时自动回退到高模，保证新增资源未减面时也能跑
- 当前依据：Vampire-like 同屏单位数量高，应优先控制单单位三角面；参考 Unity/Unreal 移动端优化建议，LOD/减面与控制整屏三角面是常规做法
- 生成结构：
- `res://assets/heroes/models_3d/<ID>/monster_profile.json`
- `res://assets/heroes/models_3d/index.json`
- 命名建议：
- `ID` 使用纯数字（如 `1001`）
- 备注可用中文，显示名形如 `1001-小骷髅`
- 当前说明：
- 该工具是独立编辑工具，不改主游戏 `run/main_scene`
- 后续英雄/怪物表可直接按 `model_id` 关联此目录中的配置 ID

### 10.1 3D配置库快照（2026-04-15）

- 校验目录：`res://assets/heroes/models_3d/`
- 校验结论：共 5 个配置，`profile_id` 与目录名一致，`index.json` 可索引，角色/武器资源路径均有效
- 已做数据规范化：所有配置已补齐 `right_weapon_* / left_weapon_*` 与 `left_weapon_tune_*` 字段（兼容老配置）

- `1001-近战低级骷髅刀兵`
- 攻击槽位：3
- 右手武器：`刀刃 | blade`
- 左手武器：空

- `1002-远程低级骷髅弩兵`
- 攻击槽位：1
- 右手武器：`弩 | crossbow`
- 左手武器：空

- `1003-近战高级骷髅斧兵`
- 攻击槽位：2
- 右手武器：`骷髅 / 战斧 | axe`
- 左手武器：`骷髅 / 战斧 | axe`

- `1004-首领骷髅法师`
- 攻击槽位：2
- 右手武器：`骷髅 / 法杖 | staff`
- 左手武器：`冒险者 / 法术书 合上 | spellbook closed`

- `2001-战士`
- 攻击槽位：3
- 右手武器：`冒险者 / 长剑 单手 | sword 1handed`
- 左手武器：`冒险者 / 盾牌 徽记 | shield badge`

### 10.2 新对话交接模板

可直接复制下面这段作为新对话首条消息：

“按 `英雄战歌2.md` 第10节继续。3D工具路径是 `res://combat_visual_editor/model_viewer/model_viewer.tscn`，配置输出在 `res://assets/heroes/models_3d/`。当前已有 5 个配置（1001/1002/1003/1004/2001），已支持统一资源池、左右手独立武器绑定、左右手独立姿态微调、攻击编排与命中帧保存。请先读取 `assets/heroes/models_3d/index.json` 与 `英雄战歌2.md` 的 10.1 快照，再继续后续开发。”

### 10.3 三渲二运行时规则（2026-04-15）

- 运行时模型源：`model_id -> data/tables/actions.json + assets/heroes/models_3d/index.json`
- 动作状态来源：`actions.json`
- `idle/move/hit/stun/death/spawn/victory`
- `actions#动作.xlsx` 以 `model_id` 为主键，状态字段直接填写 F6 动作文件名，例如 `sishuang_Run / sishuang_Jump / sishuang_Death`
- 已接入动作裁剪字段：`<状态>_start_frame / <状态>_end_frame`，目前用于 `idle/move/attack/death/jump`
- 跳跃专用字段：`jump_start_frame / jump_end_frame / jump_fps / jump_ignore_collision_frame / jump_land_frame`
- 跳跃动作可从第 X 帧开始播放，用于跳过动作文件前段多余跑动帧；碰撞盒按配置帧关闭与恢复
- 动作语法：
- 随机：`idle_1, idle_2`（也支持 `|`、`和`、`与`）
- 串播：`death_1_1 > death_1_2`
- `death` 多动作未写 `>` 时，默认按串播循环
- 编号列兼容：
- `idle_1/idle_2/...` 会自动并入随机池
- `death_1_1/death_1_2/...` 会按编号串播循环
- 攻击状态动画来源：`index.json.attack_plan`
- 攻击段落规则：
- 若存在 `windup/release/recover`，运行时按三段顺序播放并以 `release` 命中帧计算出手时机
- 若仅存在 `animation_name`，按旧单段逻辑播放（自动视为 `release`）
- 战斗渲染链路：`Model3DActorAnimator(SubViewport)` -> `Sprite2D` 输出
- 选人展示链路：`HeroModelCatalog` 会将 `Model3DActorAnimator.asset_quality` 设为 `preview`，读取高模字段
- 战斗运行链路：默认 `asset_quality = runtime`，优先读取 `runtime_*` 低模字段；低模缺失时回退高模字段
- 已统一风格化渲染参数：
- Toon 分层（默认开）
- 描边（默认开）
- 像素化采样（可选，默认关）
- 主光/补光方向与能量统一配置，可在 `model3d_actor_animator.gd` 导出参数调整

### 10.4 资产清理（2026-04-15）

- 已删除 2D 序列帧资源目录：`res://assets/heroes/sprites/`
- 已删除 2D 英雄序列帧脚本与场景残留：
- `res://hero_sprite_sheet_animator.gd`
- `res://data/hero_models/sprite_sheet_hero_model.tscn`

### 10.5 最近运行时收口（2026-04-15）

- 3D 单位对象池已接入按模型预热与复用，减少战斗中临时实例化抖动
- `F1`：范围清怪（调试）
- `F2`：直接完成当前波次（调试）
- 修复池化复用后“新刷怪保持死亡动作”问题：激活时强制重置动画运行态
- 跳跃动作已接入动作表 `jump`，支持 `jump_1/jump_2/jump_3` 三段
- 跳跃已改为位移跳跃：可跨越围怪，未落地前禁止移动/攻击
- 3D 可视化工具稳态修复（`model_viewer.tscn`）：
- 资源包可用性判定放宽为“角色目录必需、武器目录可缺省”，避免局部资源缺失时整工具不可用
- 配置 ID 解析新增分隔符容错（`-/_/—/－/:/：`），兼容不同输入习惯

## 11. Git 与交接快照（2026-04-18）

### 11.1 仓库状态（已完成）
- 本地仓库：已初始化并完成首轮基线提交
- 远程仓库：`origin = https://github.com/icemonkey69-hub/shichen.git`
- 分支状态：
- `main` 已推送并跟踪 `origin/main`
- `dev` 已创建、已推送并跟踪 `origin/dev`
- 基线标签：`baseline-2026-04-18`（已推送）
- 当前开发分支：`dev`
- 当前已完成 Git 对接；后续若改动出现问题，可基于提交记录或基线标签快速回退

### 11.2 当前提交基线
- `e1b2c50` `init: baseline`
- `6022a7d` `chore: ignore python cache files`
- `d23923f` `chore: expand gitignore for Godot workspace`

### 11.3 工程清理结论
- `E:\Godot\1122` 测试目录已删除
- 项目内未保留 `bind_dog_wolf.py / dog_bound_wolf / tmp_axis` 等 1122 相关残留引用
- 当前仓库工作区为干净状态（可直接继续开发）

### 11.4 Git 使用约定（后续统一）
1. 日常开发只在 `dev` 分支进行
2. 每次功能完成执行：
3. `git add .`
4. `git commit -m "本次改动说明"`
5. `git push`
6. 里程碑版本再打 tag 并推送 tags

### 11.5 新对话恢复口令（Git版）
“按 `英雄战歌2.md` 第11节继续，当前已对接 Git，可按提交记录或 `baseline-2026-04-18` 快速回退；当前在 `dev` 分支，远程为 `icemonkey69-hub/shichen`，先按第7节顺序推进：`MD-04` 前10波验证与结算闭环 -> `MD-05` 卡牌条件/累计被动执行 -> HUD与掉落手感统一打磨，技能系统最后接入。”

## 12. 神权 Icon 与三选一 UI 交接（2026-04-19）

### 12.1 当前结论
- 批量“6张拼图集再切图”的方案已判定不适合正式神权 icon
- 问题表现：
- 主体偏移
- 被裁切
- 相邻图串格
- 切完后不适合小尺寸游戏 icon
- 当前正式方案已切换为：
- `1张卡 = 1张单独生成的 icon`
- 最终成品固定为 `512x512`
- 生成后直接覆盖到 `res://assets/ui/icons/cards/<iconID>.png`

### 12.2 单张 Icon 正式规则（当前执行标准）
- 尺寸：`512x512`
- 主体：必须居中，完整显示，四周留安全边距
- 禁止：贴边、裁切、偏角、文字、边框、UI装饰、写实摄影、多主体、复杂场景
- 风格：低多边形、神魔幻想体系、适合游戏内小尺寸 icon
- 背景主色必须贴近品阶色：
- `1阶 = 白`
- `2阶 = 绿`
- `3阶 = 蓝`
- `4阶 = 紫`
- `5阶 = 橙`
- `6阶 = 红`

### 12.3 当前工具链
- 单张 icon 清单生成器：
- `res://tools/build_card_icon_manifest.py`
- 生成结果：
- `res://tools/card_icon_manifest.json`
- 作用：
- 从 `英雄战歌2.xlsx` 的“卡牌”页签读取名称 / 品阶 / 描述 / AI提示词
- 给每张卡生成单独 prompt
- 目标落盘路径直接对应 `iconID`

- 512 成品收口脚本：
- `res://tools/finalize_card_icon.py`
- 作用：
- 将生成结果统一落成 `512x512`
- 居中贴到最终成品画布
- 输出到 `res://assets/ui/icons/cards/<iconID>.png`

### 12.4 当前进度
- `101001` 已重新按新标准覆盖：
- 尺寸已确认是 `512x512`
- 已带入“1阶背景接近白色”规则
- `101002 ~ 101006` 曾进入单张生成阶段，但是否完全符合最新“背景贴品阶色”规则，后续应按新标准重新逐张复检或重生
- 其余历史 icon 仍可能包含旧批量图集切图残留，不能视为最终资产
- 当前正确做法不是“沿用旧图修补”，而是按 `card_icon_manifest.json` 逐张正式替换
- 旧批量图集目录 `assets/ui/icons/cards/atlases/` 与对应旧批处理脚本已删除；正式资源只保留单张 icon 流程

### 12.5 三选一 UI 当前结论
- 三选一已开始从 `game.gd` 动态拼装迁移为独立场景
- 独立场景路径：
- `res://scenes/ui/神权三选一.tscn`
- 当前节点名先使用中文，方便直接在 Godot 编辑器里拖动布局
- 当前推荐工作流：
- 先在独立场景中完成布局、锚点、自适应、裁切验证
- 再由代码层只负责填数据与交互（名字、品阶、icon、描述、刷新次数、点击选择）
- 不再继续在 `game.gd` 里硬拼布局

### 12.6 新对话建议口令（神权专项）
可直接复制下面这段作为新对话首条消息：

“按 `英雄战歌2.md` 第12节继续。当前神权 icon 已放弃图集切图方案，正式方案是 `1张卡 = 1张512x512单独生成icon`，路径为 `assets/ui/icons/cards/<iconID>.png`。请先读取 `tools/build_card_icon_manifest.py`、`tools/card_icon_manifest.json`、`tools/finalize_card_icon.py`，然后继续把 `1阶` 卡牌全部按最新规则重生成：主体居中、严格按描述、背景主色贴近品阶色（白绿蓝紫橙红）。同时三选一 UI 已迁移到独立场景 `scenes/ui/神权三选一.tscn`，后续优先在该场景里完成布局与资源接入，不再继续在 `game.gd` 中硬拼布局。” 

### 12.7 设计总表同步快照（2026-04-22）
- 本轮已确认 `res://英雄战歌2.xlsx` 已补充并更新以下核心页签：
- `英雄`：`44` 条设计（表头 `名称 / 获取方式 / 主属性 / 基础力量 / 基础敏捷 / 基础智力 / 攻击间隔 / 射程 / 法力值 / Q/W/R 技能名称与描述`）
- `卡牌 `：`219` 条设计（表头 `卡牌名称 / 卡牌等级 / 卡牌描述 / AI关键词（低多边形风格）`）
- `武器`：`250` 条设计（表头 `武器名称 / 武器品质 / 解锁条件 / 默认卡组 / 限定职业 / 名称种族 / 武器类型 / 初始冷却 / 武器效果 / 卡组介绍`）
- `存档`：`20` 条设计（表头 `类型 / 名称 / 条件 / 奖励`）
- 当前 `存档` 页签以“首通奖励 / 规则解锁 / 局外成长入口”类内容为主，用户已说明：暂时还未把“局外抽卡”完整方案并入

### 12.8 UI 拆分约定（2026-04-22）
- 当前开始执行新规则：所有主要 UI 均改为“一个界面一个独立场景”，并统一使用中文节点名，方便直接在 Godot 编辑器里自由摆放
- 第一优先界面：`res://scenes/ui/神权三选一.tscn`
- 第二优先界面：`res://scenes/ui/神权收藏.tscn`
- 战斗期运行时 UI 也已开始独立化：
- `res://scenes/ui/战斗HUD.tscn`
- `res://scenes/ui/英雄选择界面.tscn`
- `res://scenes/ui/波次信息.tscn`
- `res://scenes/ui/Boss血条.tscn`
- `res://scenes/ui/波次横幅.tscn`
- `res://scenes/ui/死亡遮罩.tscn`
- `res://scenes/ui/战斗结算面板.tscn`
- `res://scenes/ui/属性面板.tscn`
- 本轮三选一收口目标：
- 场景自身负责布局、自适应、中文文案、卡槽显示、详情区切换、刷新按钮状态
- `game.gd` 只负责抽取神权数据、应用选择结果、播放入包动画，不再继续硬拼三选一布局
- 神权收藏也按同一规则拆分：
- 场景自身负责遮罩、标题栏、排序按钮、滚动网格、详情展示与高亮切换
- `game.gd` 只负责提供已拥有神权堆叠数据、记录当前排序方式、接收关闭/排序/选中事件
- 原本直接写在 `game.tscn` 里的静态 UI 也继续场景化：
- `战斗HUD` 作为主 CanvasLayer 子场景承载肖像区 / 底部 HUD / 中央提示
- `英雄选择界面` 作为 `战斗HUD` 的子场景承载模板、血脉、预览与汇总信息
- 保持节点路径兼容现有 `game.gd`，避免拆场景时把主流程逻辑也一起打散
- 本轮拆分的原则是“只改管线，不改设计思路”，目标是让运行中的 demo 视觉表现与拆分前保持一致
- 战斗期运行时 UI 也按同一规则拆分：
- 各自场景脚本负责标题/文案/显示状态/数值显示
- `game.gd` 只负责向这些场景传入当前波次、Boss 总血量、倒计时、结算摘要与属性文本
- 后续其余 UI 也按同一模式逐个拆分，避免继续把显示逻辑堆积在 `game.gd`
- 已完成一轮旧 UI 残留清理：`game.gd` 中已移除旧三选一 / 旧神权收藏的手写布局辅助函数、旧节点引用与旧样式加载函数
- 当前 `game.gd` 保留的职责收口为：流程调度、数据组装、选择结果应用、入包动画、向独立 UI 场景喂数据
- 第二轮 UI 收口已开始落到“英雄选择界面”：
- 新增 `res://scenes/ui/英雄选择界面.gd`
- 该脚本开始负责节点缓存、静态文案、面板样式、确认按钮样式、预览区输入转发与按钮容器管理
- `game.gd` 不再直接缓存整排 `SelectionOverlay/...` 子节点，改为通过 `HeroSelectionOverlayUi` 与选择界面交互
- 当前“英雄选择”职责边界：
- `英雄选择界面.gd` 负责界面节点、按钮容器、确认事件、预览输入事件、静态 UI 收口
- `game.gd` 继续负责模板/血脉数据装配、预览模型实例化、技能说明文本、选择结果落地
- 第三轮 UI / 流程收口已继续推进到“战斗HUD + 纯文本流程”：
- 新增 `res://scenes/ui/战斗HUD.gd`
- `game.gd` 不再直接依赖 `战斗HUD` 内部的 `PortraitPanel / Panel / Message / Minimap` 子节点，改为通过 `BattleHudUi` 门面更新 HUD、消息、小地图与英雄选择显隐
- `game.gd` 中旧的 `wave_info_title_label` 直写残留已清掉，统一改为通过 `WaveInfoUi.set_wave_info()` 喂标题 / 进度 / 计时
- 新增 `res://systems/battle_result_formatter.gd`
- 战斗胜利 / 失败摘要文本已从 `game.gd` 抽到独立 formatter，避免主流程继续堆积大段结算文案
- `res://systems/wave_runtime.gd` 继续扩展为波次纯逻辑承载层，现已负责波次 HUD 状态文案、Boss 开场副标题、过渡副标题等纯文本拼装
- 第四轮运行时瘦身已继续推进：
- 新增 `res://systems/combat_info_formatter.gd`，战斗 HUD 文本格式化从 `game.gd` 抽离
- 新增 `res://systems/kill_reward_amount_calculator.gd`，击杀金币/经验最终结算公式只保留一个入口
- 新增 `res://systems/reward_pickup_splitter.gd`，掉落物金额拆分从主流程抽离
- 新增 `res://systems/threshold_reward_picker.gd`，击杀阈值奖励的选卡/选武器逻辑从 `game.gd` 抽离
- 新增 `res://systems/card_icon_resolver.gd`，卡牌 icon 解析、fallback、贴图缓存从 `game.gd` 抽离
- 新增 `res://systems/table_value_utils.gd`，表格可选 ID、ban 开关、float 常量解析集中处理
- 新增 `res://systems/card_display_text.gd`，三选一、收藏、入包动画共用卡牌占位文字规则
- 新增 `res://systems/card_description_text.gd`，卡牌描述拆行、被动前缀识别、忽略行规则、被动前缀剥离从 `game.gd` 抽离
- 新增 `res://systems/regex_cache.gd`，卡牌运行时正则匹配复用已编译表达式，减少重建被动时的重复 `RegEx.compile()`
- 新增 `res://systems/transient_message_runtime.gd`，中央短提示的倒计时与排队播放从 `game.gd` 抽离，主流程只负责把消息交给 HUD 显示/隐藏
- `game.gd` 已收拢 `player.get_combat_stats()` 查询入口，减少重复判空与 `has_method` 分支
- 已删除一批确认无人使用的旧辅助函数，减少冗余：
- `game.gd::_build_selection_description_text()`
- `game.gd::_build_bloodline_visual_summary()`
- `game.gd::_build_wave_spawn_progress_text()`
- `game.gd::_get_active_boss_nodes()`
- 本轮目标仍然保持不改 demo 视觉与规则，仅继续降低 `game.gd` 的节点依赖、重复文案、重复计算与无效辅助逻辑
- 解析校验：`E:\\Godot\\Godot_v4.6.1-stable_win64_console.exe --headless --path E:\\Godot\\vampire-like-demo --quit-after 5`
- 神权战斗节奏已新增正式运行时入口：
- `game.gd` 现在按 `runtime_constants.json` 的卡包参数表进行“冷却到点 -> 概率判定 -> 掉落神权机会”流程；当前测试值为 `5` 秒 / `100%` / 失败后 `+4%`
- 首个神权机会掉落时间已调整为 `0` 秒：战斗正式开始后即允许立刻生成第一份神权机会
- 神权机会不再在计时点直接弹出三选一，而是先掉落到玩家附近地面，沿用现有 `pickup` 磁吸/拾取管线
- 掉落时当前不额外弹提示字，主要依靠地面光圈 / 呼吸高亮 / 拾取物自身表现来提示玩家
- 玩家拾取该掉落物后，才会打开现有 `神权三选一` 界面
- 神权机会拾取后当前也不额外弹文字提示，改为短促的拾取闪光特效 + 极短延迟后打开三选一
- `神权三选一` 打开前当前会追加一层很轻的金色聚焦脉冲，弹窗自身也会做一次淡入 + 轻微放大过渡
- 若玩家连续拾取多个神权机会，会按次数排队依次打开三选一，不会吞掉额外机会
- `pickup.gd / pickup.tscn` 已扩展出 `card_choice` 拾取类型，当前先用自定义 `Snk` 占位视觉配合卡框表现“神权机会”，后续可直接替换正式资源
- 死亡 / 选人 / 暂停类界面期间，战斗计时不推进；因此神权机会掉落计时也会同步冻结
- 当前允许地面同时存在多个未拾取的神权机会，不强制场上唯一
- 已新增正式源表 `Excel/runtime_constants#运行时常量.xlsx` -> `data/tables/runtime_constants.json`，当前先约定：
- `ID=1`：卡包掉率冷却CD
- `ID=2`：卡包掉率初始概率
- `ID=3`：卡包掉率提升概率值
- 表字段当前统一为：`id / key / name / value / notes`
- 运行时代码会优先读取该表；表缺失或值非法时回退到 `game.gd` 导出默认值
- 当前神权机会判定已改为“到冷却点后按概率判定一次”；成功掉落后概率重置为初始概率，失败则按提升值累加到下一次判定
- 当前仍保持：三选一界面本身、刷新次数、选择后入包动画与神权收藏按钮逻辑不变；本轮只调整“机会出现方式”
- `神权三选一` 当前已补入一套可替换的新版位图资源，并优先接到运行时：
- 背景：`assets/ui/card_choice/backgrounds/god_demon_choice_bg_v1.png`（已直接覆盖为新版内容）
- 背景动效：已按验收结论删除；当前三选一只使用静态背景图 `res://assets/ui/card_choice/backgrounds/god_demon_choice_bg_v1.png`
- 卡框：`assets/ui/card_choice/frames/tier_1_white.png` ~ `tier_6_red.png`（已直接覆盖为新版内容）
- 刷新按钮：`assets/ui/card_choice/buttons/refresh_button_normal.png / refresh_button_hover.png / refresh_button_disabled.png`
- 刷新按钮当前已改为命运齿轮祭坛风格三态图，运行时显示为 `116x116` 方形，中心暗色铭牌用于承载“刷新”文字
- 选择高亮：当前不再使用卡框流光/序列帧覆盖层，改为 shader 根据卡框透明通道生成边缘高亮、斜向扫光、点击爆闪，并配合卡牌轻微抬起
- 资源拆分目标是支持后续按单张文件替换，不需要每次一起重做整套 UI 图
- 当前卡框资源已按“标题 / 图标 / 阶级 / 描述都在框内完成”的安全区重新接入；紫橙红比白绿蓝更华丽，但仍控制在根框体轮廓内，避免 `3/4/5/6` 选一自适应时破版

### 12.9 三选一资产规格收口（2026-04-26）
- 三选一资源不再自由生成，正式规格以 `res://assets/ui/card_choice/ASSET_SPEC.zh-CN.md` 为准
- 基准分辨率：`1440x810`
- 三选一主面板显示尺寸：`1220x660`
- 单张卡槽显示尺寸：`300x380`
- 卡牌 icon 固定源图：`512x512`，主体居中，中心 `400x400` 为主体安全区，最低留 `48px` 边距；三选一卡面内显示尺寸以 `神权三选一.tscn` 可视化摆放为准
- 卡框统一源图尺寸：`900x1140`，运行时缩放到 `300x380`
- 卡框名称区与描述区必须使用偏灰/偏暗底色，避免白色字体在白色品阶卡框上不可读
- 六阶卡框必须按品阶独立设计，不允许只由白框换色得到；白阶克制银白，绿阶加入自然藤蔓，蓝阶加入冰晶/符文，紫阶加入奥术虚空，橙阶走金橙圣焰，红阶走黑红熔岩/尖刺，稀有度越高装饰层级越强
- 当前卡牌选中反馈已去掉整块蒙版覆盖层，也不再使用 `selection_glows` 位图序列帧；改为 `res://assets/shaders/card_choice_selected_highlight.gdshader` 复用当前卡框贴图的透明通道生成边缘高亮、斜向扫光和点击爆闪，避免选中后出现整块强光遮挡卡面
- 六阶卡框已按统一安全区重画：icon 窗、标题条、描述框共用同一坐标；宝石、徽记、尖刺、羽翼、藤蔓、晶体等装饰不得进入标题/描述文字区
- 背景统一源图尺寸：`1920x1080`，运行时按 `KEEP_ASPECT_COVERED` 裁切覆盖
- 刷新按钮统一源图尺寸：`256x256`
- 后续替换资源时，先统一源图尺寸与安全区，再接入场景；不允许每阶卡框各自不同尺寸
- `res://scenes/ui/神权三选一.tscn` 是视觉真源：编辑器里看到的布局必须等于游戏内运行布局
- `game.gd` 只负责传入卡牌数据、刷新次数、选择结果与入包动画，不再写三选一卡面布局
- 已按 Godot 正规 prefab 流程新增 `res://scenes/ui/神权三选一卡牌.tscn`；`神权三选一.tscn` 中的 `卡牌一..卡牌九` 均实例化该卡牌场景
- 三选一运行时不再用脚本常量重写 icon、名称、描述坐标和尺寸；这些位置以 `神权三选一卡牌.tscn` 的可视化摆放为准
- 当前名称与描述文本已改为运行时自适应字号，必须限制在 prefab 中可视化画好的文字区域内，不允许越界覆盖卡框装饰

### 12.10 三选一美术资产生成规则（2026-04-26）
- 正式背景图路径固定为：`res://assets/ui/card_choice/backgrounds/god_demon_choice_bg_v1.png`
- 背景只使用 `1920x1080` 静态整图，运行时按 `KEEP_ASPECT_COVERED` 裁切覆盖主面板；当前不再接入背景动效
- 背景画面要求：左侧偏神圣/冷光，右侧偏地狱/熔岩，中间留出三张卡牌和标题的阅读空间，禁止把关键主体放到会被卡牌遮挡的位置
- 卡框路径固定为：`res://assets/ui/card_choice/frames/tier_1_white.png` ~ `tier_6_red.png`
- 卡框源图必须是 `900x1140` 透明 PNG；六阶为白、绿、蓝、紫、橙、红，必须独立设计，不允许只做白框换色
- 卡框真实可见外接框必须六阶一致，透明边距必须统一；否则 Godot 同尺寸 TextureRect 显示时会产生视觉错位
- 卡框安全区必须统一：Icon、标题、描述区域不得被宝石、尖刺、羽翼、藤蔓、晶体等装饰压住
- 刷新按钮路径固定为：`res://assets/ui/card_choice/buttons/refresh_button_normal.png / refresh_button_hover.png / refresh_button_disabled.png`
- 刷新按钮源图必须是 `256x256` 透明 PNG，运行时显示 `116x116`；当前采用命运齿轮/神权祭坛风格，中间暗色铭牌承载“刷新”文字
- 选中反馈不再生成位图序列帧，统一使用 `res://assets/shaders/card_choice_selected_highlight.gdshader` 根据当前卡框透明通道生成边缘高亮、斜向扫光和点击爆闪
- 卡牌名称和描述文字禁止画进美术图；由运行时 Label 填充并自适应字号
- 卡牌 icon 正式源图固定 `512x512`，一张卡一个独立 icon，主体居中，中心 `400x400` 为主体安全区，最低留 `48px` 边距
- 正式目录只保留当前场景直接引用的资源：`backgrounds / buttons / frames / icons`；临时预览、备份、源图、试验性 overlay 必须在验收后删除

## 13. 下一阶段：局内金币成长武器（剑&盾）

### 13.1 新阶段目标
- 三选一阶段当前告一段落；下一阶段转入 `MD-03 武器系统`
- 首个目标：实现 `剑&盾` 的“局内可花金币成长武器”玩法闭环
- 该功能应优先做成可玩原型：玩家在局内消耗金币购买/升级剑与盾，立即影响战斗属性或战斗表现，并能在 HUD/UI 中看到等级、花费和成长结果

### 13.2 需要优先读取的资料
- 源表目录：`res://Excel/`
- 武器源表：`res://Excel/weapons#武器.xlsx`
- 导出目标：`res://data/tables/weapons.json`
- 血脉源表：`res://Excel/bloodlines#血脉.xlsx`
- 剑盾血脉：`data/tables/bloodlines.json` 中 `id=2001`，`name=剑盾`，`model_id=2001`
- 当前金币字段：`game.gd::current_gold`
- 当前武器行加载入口：`game.gd::_load_weapon_rows()`
- 当前武器被动属性入口：`game.gd::_build_weapon_bonus_values()`
- 当前金币获得/消耗相关现状：金币已能通过怪物奖励、拾取物、每秒金币、杀敌金币等方式进入 `current_gold`
- 当前模型/武器外观配置：`res://assets/heroes/models_3d/index.json`，其中 `2001` 为剑盾配置，右手长剑、左手盾牌

### 13.3 已有可复用基础
- `MD-05.5` 已完成金币/经验掉落、吸附、到账、HUD 同步、属性面板同步
- `player.gd` 已支持 `runtime_bonus_values` 动态刷新最终属性
- `game.gd` 已有奖励系统把武器转成被动属性加成的临时入口
- `data/tables/weapons.json` 当前已有少量武器样例行，但正式实现前应以 `Excel/weapons#武器.xlsx` 为准重新确认字段
- 3D 模型系统已支持左右手武器绑定，剑盾血脉外观配置可复用

### 13.4 实现边界建议
- 第一版只做 `剑&盾`，不要一次扩展所有武器类型
- 第一版优先做局内成长闭环，不急着做复杂美术
- 建议先实现三块：
- 数据：从 `weapons#武器.xlsx` 或新增/扩展字段读取剑、盾成长配置，如初始等级、升级花费、成长曲线、每级属性
- 逻辑：消耗 `current_gold` 升级剑/盾，刷新玩家属性或武器效果，金币不足时给 UI 反馈
- UI：新增一个小型局内武器成长面板或入口，显示剑/盾等级、当前效果、下级效果、升级花费、升级按钮
- 若需要新增表，优先新增清晰独立表，例如 `weapon_growth#武器成长.xlsx`；不要把临时规则硬塞进 `game.gd`
- 若暂不新增表，可先在 `weapons#武器.xlsx` 中扩展字段，但必须同步导出 JSON 并写入字段说明

### 13.5 验收标准
- 进入战斗后能看到剑&盾成长入口
- 当前金币足够时，点击升级能扣除金币
- 升级后剑/盾等级增加，UI 立即刷新
- 升级后对应属性或战斗效果立即生效，并能在属性面板或战斗表现中验证
- 当前金币不足时不能升级，并有明确反馈
- 重新开局时局内剑/盾成长应按本局规则重置，除非后续明确接入局外永久成长

### 13.6 新对话恢复口令（局内武器成长）
复制下面这段作为新对话第一条消息：

“按 `英雄战歌2.md` 第13节继续。三选一阶段已收口，当前不要再改三选一 UI。现在开始实现 `MD-03 武器系统` 的第一版：局内可花金币成长武器 `剑&盾`。请先读取 `Excel/weapons#武器.xlsx`、`data/tables/weapons.json`、`data/tables/bloodlines.json`、`game.gd` 中金币与武器相关函数、`assets/heroes/models_3d/index.json` 的 `2001` 剑盾配置；然后提出并实现一个最小可玩闭环：局内显示剑/盾成长入口，消耗 `current_gold` 升级，刷新属性或战斗效果，金币不足有反馈，并确保本局重开时成长重置。”

## 14. 站台守护者核心玩法调整案（2026-04-30）

### 14.1 定稿方向
- 项目核心从“英雄幸存者”调整为“2.5D 俯视角防御幸存者 / 站台守护者”
- 运行时旧“英雄”定义为地图中心固定防御塔，塔是主要输出、主要成长、主要胜负目标
- 英雄选择界面暂不改名，本质上选择的是塔模板与塔专精；选完后生成一个可移动守护者
- 守护者是玩家操作单位，负责走位、拾取掉落、踩符文、触发站台事件、通过 UI 释放塔技能
- 第一阶段守护者无敌，不设计独立血蓝，先把改动范围控制到最小
- 敌人第一阶段全部优先攻击塔；普通怪/Boss 的复杂仇恨、特殊追玩家怪、陷阱伤害等后续再扩展
- 塔生命归零即失败；Boss 波超时未击杀仍失败

### 14.2 已确认规则
- 玩家/守护者不能普攻；当前玩家攻击链路后续迁移为塔自动攻击
- Q/R 技能释放通过 UI 或快捷键触发，但释放主体只可能是塔，位置、范围、判定均以塔为准
- 神权三选一逻辑暂不改名；拾取机会由守护者跑过去捡，获得后的属性仍加给塔
- 剑&盾成长逻辑暂不改名；升级 UI 和规则沿用现有设计，属性加给塔
- 武器槽规则暂不改：初始 3 个武器槽，后续可通过局外/局内临时因素扩展
- 附魔类效果：伤害/减益以守护者周围产生；增益效果守护者和塔同时受益
- 符文生成规则暂不实现复杂化；后续放入常量表，例如初始生成半径 `2000`，再由卡牌/技能缩小或改变规则
- 波次连续推进沿用当前规则，不额外引入安全时间
- 系统显示名称暂不改，后续做多语言时统一处理：
- 英雄/职业/血脉/武器/神权/剑盾等当前 UI 文案先保留
- 内部实现先按“塔/守护者/站台事件/祭坛/战利品”设计，但不急着重命名表字段和旧节点

### 14.3 第一阶段最小可玩迁移
1. 固定旧英雄为中心塔：不可移动，继续承载当前属性、生命、武器、神权、剑盾成长、经验/金币加成
2. 新增守护者移动单位：由玩家 WASD 控制，第一阶段无敌，无普攻，主要用于拾取和未来跑图交互
3. 敌人目标改为塔：第一阶段所有敌人都优先攻击塔
4. 拾取物吸附目标改为守护者：金币、经验、神权机会等由守护者跑位拾取，但收益写入塔/当前局属性体系
5. 玩家攻击迁移给塔：当前普攻/投射物链路逐步从移动玩家身上移到中心塔自动释放
6. 技能主体统一为塔：未来 Q/R 输入与 UI 按钮只触发塔技能，判定位置按塔中心
7. 保持现有 UI 文案与表字段：先完成规则迁移，再统一做包装名、多语言与视觉表现

### 14.3.1 第一版实现快照
- 新增 `res://guardian.gd` 与 `res://guardian.tscn`，作为第一阶段守护者占位单位
- 守护者由 WASD/方向键移动，带独立相机，第一阶段无敌且 `receive_damage()` 为空实现
- 守护者视觉已从橙色菱形占位替换为 `Tiny Swords (Free Pack)/Units/Black Units/Warrior` 的 2D Warrior，当前接入 Idle/Run 帧表动画；守护者仍无普攻，攻击帧暂不使用
- 塔继续沿用现有英雄模型、待机、攻击、死亡等表现，只禁止移动
- `game.tscn` 新增 `Guardian` 节点；战斗开始后守护者在塔下方生成并接管相机
- `player.gd` 新增塔模式：旧英雄不再移动，但保留生命、属性、受击、自动寻敌和投射物攻击
- 塔逻辑锚点固定为战场 `play_area` 中心；塔模式下会检测逻辑位置是否偏离锚点，只有偏离时才回正，未来位移演出只能移动视觉层，演出结束后仍回到中心点
- 敌人仍接收旧 `player` 作为目标，因此第一阶段会直接追击并攻击中心塔
- 掉落物吸附目标改为守护者；金币、经验、神权机会拾取后仍写入当前局/塔属性体系
- 塔死亡直接判定失败，不再走旧玩家复活流程；守护者死亡/复活机制暂不接入
- 本轮验证命令：`Godot_v4.6.1-stable_win64_console.exe --headless --path E:\Godot\vampire-like-demo --quit-after 5`

### 14.3.2 草地战场接入与点位规范（2026-05-01）
- `game.tscn` 的初始战斗地形切换为 `res://scenes/world/草地.tscn`，实例节点名为 `战斗地形`
- `草地.tscn` 根节点挂载 `res://scenes/world/草地战斗地形.gd`，负责暴露 `play_area`、塔出生点、Boss 出生点、普通刷怪点
- 点位统一使用 `Marker2D`，节点名使用中文强约束：`塔出生点` 为塔出生点，`Boss出生点` 为 Boss 出生点，`普通怪刷怪点` 是普通怪刷怪点分组
- 当前 `普通怪刷怪点` 下预置 4 个 `Marker2D`：`上方刷怪点`、`右侧刷怪点`、`下方刷怪点`、`左侧刷怪点`
- 点位命名是运行时规则，不是纯文案：脚本按上述中文名直接读取节点；缺少必要节点时让 Godot 报错，不再隐藏问题并退回默认点
- 后续新增刷怪点时，直接在 `普通怪刷怪点` 下添加新的 `Marker2D` 即可，名称可按方位/用途命名，例如 `左上刷怪点`、`精英刷怪点01`
- 普通怪刷怪从 `普通怪刷怪点` 的所有 `Marker2D` 中走随机袋：每轮会随机顺序使用所有刷怪点一次，用完再重洗，不再按相机可视区域过滤，避免只从某个方向连续刷怪
- 边界规则交给场景碰撞盒处理：守护者和敌人都是 `CharacterBody2D + CollisionShape2D`，根节点显式使用 `collision_layer=1`、`collision_mask=1`
- 守护者不再使用 `play_area` 做移动硬限制；`play_area` 只作为战斗区域/小地图/后续系统参考数据，不作为玩家移动边界
- 草地 TileMap 碰撞由场景本身决定，脚本不再运行时关闭 TileMap 碰撞；需要阻挡的边界/水域/障碍请在 `草地.tscn` 内维护碰撞层
- 旧程序绘制测试地形已删除：`res://scenes/world/测试地形.tscn`、`res://scenes/world/战斗地形.gd`

### 14.4 暂缓事项
- 守护者血量、法力、受伤、死亡与复活机制暂缓；第一阶段守护者无敌
- Boss 复杂仇恨：战宠 > 塔 > 玩家，暂缓到战宠/护卫遗物正式接入后处理
- 站台四角事件暂缓到核心塔/守护者闭环稳定后实现
- 符文生成半径、符文缩圈卡牌、光环覆盖高级规则暂缓到技能/遗物系统阶段
- 所有包装名替换和多语言文本暂缓，避免当前阶段大面积改 UI 文案

### 14.5 新对话恢复口令（站台守护者）
“按 `英雄战歌2.md` 第14节继续。当前玩法已确认调整为站台守护者：旧英雄运行时定位为中心塔，守护者是新玩家移动单位且第一阶段无敌、无普攻，只负责拾取和跑图交互；敌人第一阶段全部优先攻击塔；神权/剑盾/武器槽/波次规则先不改名不改表字段，只把属性和收益落到塔。请先实现最小可玩迁移：固定旧英雄为塔、新增守护者移动拾取、敌人攻击塔、玩家攻击链路迁移为塔自动攻击，并保持每小步 Godot 验证、Git 提交、同步 MD。”
