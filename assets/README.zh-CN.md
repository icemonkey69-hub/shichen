# assets 说明

这里是游戏实际运行时使用的美术资源目录。

规则：
- 放在这里的图片会被 Godot 导入，所以会生成 `.import` 文件
- 程序、表格、场景引用的资源路径都应指向这里
- 素材分类尽量按现有目录放，方便策划和程序一起维护

当前分类：
- `heroes/`：英雄相关资源
- `enemies/`：敌人相关资源
- `projectiles/`：子弹和飞行物
- `weapons/`：武器和武器图标
- `ui/`：界面图标
