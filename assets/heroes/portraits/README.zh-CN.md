# 英雄头像目录

这里放英雄头像资源。

## 规则

- 头像文件放在这里
- 在 `Excel/heroes#英雄.xlsx` 里填写 `portrait_icon`

例如：

- `portrait_icon = res://assets/heroes/portraits/hero_portrait_default.svg`

导出到 `data/tables/heroes.json` 后，程序会读取这个路径。
