# 开发协作说明（当前版）

## 当前分工
- 你负责：玩法策划、数值配置、资源挑选与验收
- 我负责：代码实现、读表接入、运行时联调与清理

## 当前数据流
1. 策划维护：`res://英雄战歌2.xlsx`
2. 项目源表：`res://Excel/*.xlsx`
3. 一键导表：`res://Excel/一键导出JSON.bat`
4. 运行时读取：`res://data/tables/*.json`

## 美术与模型资源目录
- 3D模型配置：`res://assets/heroes/models_3d/index.json`
- 3D模型运行时动作表：`res://data/tables/actions.json`
- 英雄头像：`res://assets/heroes/portraits/`
- 怪物与英雄战斗表现：`Model3DActorAnimator`（三渲二）

## 模型规则（已改为纯3D）
- `model_id` 对应 `assets/heroes/models_3d/index.json` 中的 `profile_id`
- 非攻击状态动画从 `actions.json` 读取：
- `idle/move/hit/stun/death/spawn/victory`
- 攻击动画与命中帧从 `index.json.attack_plan` 读取

## 约定
- 继续维护 `xlsx`，不再维护 `csv`
- 运行时统一读取 `json`
- 2D序列帧链路已下线，不再使用 `assets/heroes/sprites/`
