# 开发协作说明（当前版）

## 当前分工

- 你负责：玩法策划、数值配置、资源挑选与验收
- 我负责：代码实现、读表接入、运行时联调与清理

## 当前数据流

1. 策划维护：`res://英雄战歌2.xlsx`
2. 项目源表：`res://Excel/*.xlsx`
3. 一键导出：`res://Excel/一键导出JSON.bat`
4. 运行时读取：`res://data/tables/*.json`

## 当前 2D 美术管线

- 敌人：`res://assets/enemies/Models_2d/<ID>_备注/`，ID 对应 `enemies.model_id`
- 守护者：`res://assets/heroes/Models_2d/<ID>_备注/`，ID 对应 `bloodlines.model_id`
- 塔：`res://assets/heroes/point_2d/<ID>_备注/`，ID 对应 `bloodlines.model_id_point`
- 地形：当前草地仍直接引用 `Tiny Swords (Free Pack)/Terrain` 中的 TileSet 图片

## 约定

- 旧 3D / 三渲二 / F6 动作表管线已废弃
- 新增单位资源时，只需要新增对应 ID 文件夹并配置表中 ID
- 文件夹名 `_` 后内容只做备注，不参与运行时匹配
- 运行时不直接读取 `xlsx` 或 `csv`，统一从 `data/tables/*.json` 读取
