# 开发协作说明（当前版）

## 当前分工

- 你负责：玩法策划、数值配置、资源挑选与验收。
- 我负责：代码实现、读表接入、运行时联调与清理。

## 当前数据流

1. 策划维护：`res://英雄战歌2.xlsx`
2. 项目源表：`res://Excel/*.xlsx`
3. 一键导出：`res://Excel/一键导出JSON.bat`
4. 运行时读取：`res://data/tables/*.json`

## 美术与模型资源目录

- 原始角色/怪物白模与动作：`E:/Godot/model_3D/Ai_Model/<角色文件夹>/`
- 原始武器模型：`E:/Godot/model_3D/Ai_weapons/`
- F6 模型配置输出：`res://assets/heroes/models_3d/`
- 英雄头像：`res://assets/heroes/portraits/`

## 模型规则

- `model_id` 对应 `assets/heroes/models_3d/index.json` 中的配置 ID。
- 每个角色文件夹必须有一个 `*_benti.glb` 或 `*_benti.fbx` 作为本体。
- 同文件夹下其他 GLB/FBX 都作为动作文件读取，包括 `*_Idle`。
- F6 用于预览、绑定左右手武器、调挂点、调攻击帧，并保存给游戏运行时读取。

## 约定

- 继续维护 `xlsx`，不维护 `csv`。
- 运行时统一读取 `json`。
- 新增英雄/怪物 ID 时，优先通过 F6 生成或更新 `models_3d/index.json`。
